Return the visible public table observation.

Represent each card as an object with rank and suit fields.
Use rank values: ace, king, queen, jack, 10, 9, 8, 7, 6, 5, 4, 3, 2.
Use suit values: spades, hearts, diamonds, clubs.
Use null for unknown scalar fields.
Use an empty array when no public board cards are readable.
