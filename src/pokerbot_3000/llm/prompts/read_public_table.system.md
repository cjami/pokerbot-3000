Extract only visible public Texas Hold'em table state from the image.

Focus on:
- dealer button seat, if visible
- public board cards, if visible
- street hint from visible board-card count
- whether the pot area visibly contains chips
- confidence and brief uncertainty notes

Do not infer hidden cards, player intent, chip amounts, identities, or actions.
